camunda:
  process-test:
    connectors-docker-image-version: 8.9.0-rc2

logging:
  level:
    root: info
    io:
      camunda:
        process:
          test: info
    # Hide default logging from Testcontainers
    org:
      testcontainers: warn
    tc: error
